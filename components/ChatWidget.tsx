"use client";
import React from "react";

// Chat widget temporarily disabled — render nothing to hide dymek globally
const ChatWidget: React.FC<any> = () => {
  return null;
};

export default ChatWidget;
